syntax = "proto3";
package cart;

service cart {
    rpc addClass(classRequest) returns (classResponse);
  }
  
message classRequest {
  string userName = 1;
  string courseCode = 2;
  string section = 3;
}
message classResponse {
  bool success = 1;
}
